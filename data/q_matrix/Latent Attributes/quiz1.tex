Translating logic into sets.
Probability rules (e.g. Pr(not A) =1 - Pr(A))
Bounding Chances (e.g. Pr(A or B) \leq Pr(A) + Pr(B))

4. Relating Joint, Marginals, and Conditionals (e.g. Pr(A|B) = Pr(A,B)/Pr(B))
5. Computing probability by counting given equally likely outcomes.

